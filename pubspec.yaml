name: kentochat
description: КентоЧат - личные сообщения
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^2.0.1
  shared_preferences: ^2.2.0
  image_picker: ^1.0.4
  intl: ^0.19.0
  http: ^1.1.0
  file_picker: ^5.5.0
  cached_network_image: ^3.3.0
  video_player: ^2.8.0

flutter:
  uses-material-design: true